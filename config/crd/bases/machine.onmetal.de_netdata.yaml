
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.5.0
  creationTimestamp: null
  name: netdata.machine.onmetal.de
spec:
  group: machine.onmetal.de
  names:
    kind: Netdata
    listKind: NetdataList
    plural: netdata
    singular: netdata
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.hostname[*]
      name: Hostnames(only first)
      type: string
    - jsonPath: .spec.addresses[?(@.iptype == 'ipv4')].ips[*]
      name: IPv4
      type: string
    - jsonPath: .spec.addresses[?(@.iptype == 'ipv6')].ips[*]
      name: IPv6
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Netdata is the Schema for the netdata API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetdataSpec defines the desired state of Netdata
            properties:
              addresses:
                items:
                  properties:
                    ips:
                      items:
                        type: string
                      minItems: 1
                      type: array
                    iptype:
                      default: '''ipv4'''
                      type: string
                    subnet:
                      type: string
                  required:
                  - ips
                  - iptype
                  - subnet
                  type: object
                minItems: 1
                type: array
              expiration:
                format: date-time
                type: string
              hostname:
                items:
                  type: string
                type: array
              macAddress:
                pattern: ^([0-9A-Fa-f]{2}[:-]){5}([0-9A-Fa-f]{2})$
                type: string
            type: object
          status:
            description: NetdataStatus defines the observed state of Netdata
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
